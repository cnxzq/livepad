services:
  livepad:
    image: zqzyz/livepad
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
    volumes:
      - livepad-data:/tmp/.livepad
    restart: unless-stopped

volumes:
  livepad-data:
