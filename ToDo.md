# 待办事项

* [x] 添加参数用来指定密码：`--password <密码>`
* [x] 添加参数指定空密码：`--no-password` 或 `--password=`，免密码访问
* [x] 当前文字内容也要持久化：保存到 `.livepad-text.json`，写盘成功后再确认同步
* [x] 默认启动不清空之前存储、文本：默认恢复；`--clear` 显式清空，`--keep` 兼容保留
